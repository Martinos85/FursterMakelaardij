// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://www.furstermakelaardij.nl', // Uw eigen domein
  base: '/',                           // Gewijzigd naar '/' omdat het nu de hoofdmap is
});

